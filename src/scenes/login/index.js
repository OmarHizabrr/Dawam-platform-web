import React from 'react';
import {SafeAreaView, Text, StatusBar,Image} from 'react-native';

const LoginScreen = () => (
  <SafeAreaView>
    <View>Screen: Login</View>
  </SafeAreaView>
);
export default LoginScreen;
