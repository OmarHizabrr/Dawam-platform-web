# 📊 التقرير التحليلي الشامل - نظام أرصدة الإجازات

## 🎯 الهدف من التحليل

فهم آلية عمل نظام إدارة أرصدة الإجازات بشكل كامل وتحديد ما هو موجود حالياً وما يحتاج إلى تطوير.

---

## 📌 ما هو موجود حالياً في النظام

### 1. **واجهة عرض أرصدة الإجازات** ✅

#### المكونات:

- **جدول ديناميكي** يعرض:
  - اسم الموظف
  - الإدارة (Category)
  - الوظيفة (Job)
  - أرصدة كل نوع إجازة (ديناميكي حسب أنواع الإجازات)
  - زر تعديل لكل موظف

#### المميزات:

- ✅ فلترة حسب اسم الموظف (مع بحث)
- ✅ فلترة حسب الإدارة (مع بحث)
- ✅ ترتيب (Sorting)
- ✅ اختيار السنة (DatePicker)
- ✅ طباعة التقرير
- ✅ تصدير إلى Excel

#### الكود:

```javascript
// السطر 1363-1369
<Table
  loading={load}
  columns={getColumnsVac()}
  scroll={{ x: "1000px" }}
  dataSource={pdata}
  onChange={handleChange}
/>
```

---

### 2. **نموذج إضافة رصيد إجازة** ✅

#### الهيكل:

- **قسم الإعدادات الأساسية** (النموذج العلوي):

  - نوع الإجازة (Select)
  - نوع الرصيد (Select)
  - نوع الفترة: شهرية/سنوية (Select)
  - الرصيد بالدقائق (InputNumber)
  - ملاحظات (Input)
  - زر "جلب الموظفين" (لجلب الموظفين المدينين)

- **قسم تفاصيل الموظفين** (نموذج ديناميكي):
  - يمكن إضافة/حذف موظفين
  - لكل موظف:
    - اسم الموظف (Select)
    - نوع الإجازة (Select)
    - نوع الرصيد (Select)
    - الرصيد بالدقائق (InputNumber)
    - نوع الفترة (Select)
    - ملاحظات (TextArea)

#### المميزات:

- ✅ إضافة رصيد لموظف واحد أو عدة موظفين
- ✅ تحديد نوع الفترة (شهرية/سنوية)
- ✅ التحقق التلقائي: عند اختيار إجازة سنوية، يتم تعيين نوع الفترة تلقائياً إلى "yearly"

#### الكود:

```javascript
// السطر 765-1208
<Modal title="إضافة رصيد إجازة موظف">
  {/* النموذج العلوي */}
  <Form form={gform}>{/* الإعدادات الأساسية */}</Form>

  {/* النموذج الديناميكي */}
  <Form form={form}>
    <Form.List name="tasks">{/* قائمة الموظفين */}</Form.List>
  </Form>
</Modal>
```

---

### 3. **نموذج خصم رصيد إجازة** ✅

#### الحقول:

- اسم الموظف (Select)
- نوع الإجازة (Select)
- مقدار الخصم بالدقائق (InputNumber)
- تاريخ الخصم (DatePicker)
- ملاحظات (TextArea)

#### الكود:

```javascript
// السطر 1210-1296
<Modal title="خصم رصيد إجازة">
  <Form form={dform}>{/* الحقول */}</Form>
</Modal>
```

---

### 4. **التقرير السنوي** ✅ (موجود لكن غير مستخدم)

#### البيانات المعروضة:

- **مرحل من العام الماضي** (`prev`): الرصيد المتبقي من السنة السابقة
- **رصيد العام الحالي** (`curr`): الرصيد الممنوح للسنة الحالية
- **رصيد محول** (`trans`): رصيد محول من إجازة أخرى
- **الافتتاحي** (`op`): مجموع (prev + curr + trans)
- **الاستهلاك الشهري**: من m1 إلى m12 (يناير إلى ديسمبر)
- **الممنوح**: إجمالي الممنوح
- **المتبقي**: الرصيد المتبقي

#### المشكلة:

- ❌ التقرير موجود في الكود (السطر 1632-1988)
- ❌ لكن لا يوجد زر لطباعته في الواجهة
- ❌ الدالة `printAnnualyReport()` موجودة لكن غير مستخدمة

#### الكود:

```javascript
// السطر 519-540
function printAnnualyReport() {
  setLoadReport(true);
  var report = document.getElementById("ann-report");
  // ... طباعة التقرير
}
```

---

## ❌ ما هو غير موجود (يحتاج تطوير)

### 1. **نظام ترحيل الإجازات إلى السنة الجديدة** ❌

#### الوضع الحالي:

- ❌ لا توجد وظيفة لترحيل أرصدة الإجازات تلقائياً
- ❌ يجب الترحيل يدوياً لكل موظف
- ❌ لا يوجد سجل للترحيلات السابقة

#### المطلوب:

- ✅ زر "ترحيل الإجازات إلى السنة الجديدة"
- ✅ API endpoint للترحيل
- ✅ واجهة اختيار:
  - السنة المصدر (من)
  - السنة الهدف (إلى)
  - أنواع الإجازات المراد ترحيلها
  - الموظفين (كلهم أو محددين)
- ✅ معاينة قبل الترحيل
- ✅ تأكيد العملية
- ✅ تقرير بعد الترحيل

---

### 2. **تحسينات على الكود الحالي** ⚠️

#### المشاكل الموجودة:

##### أ. بناء JSON يدوياً (غير محسن):

```javascript
// السطر 219-264
var vacData = "[";
emp.map((user, index) => {
  vacData += "{" + '"empName":"' + user.label + '",...';
});
var json = JSON.parse(vacData.substring(0, vacData.length - 1) + "]");
```

**المشكلة**:

- معرض للأخطاء
- صعب الصيانة
- غير فعال

**الحل المقترح**:

```javascript
const records = emp.map((user) => {
  // بناء الكائن مباشرة
  return {
    empName: user.label,
    user_id: user.value,
    // ...
  };
});
```

##### ب. عدم وجود تحقق كافٍ من البيانات:

- ❌ لا يوجد تحقق قبل الإرسال
- ❌ رسائل خطأ غير واضحة

##### ج. معالجة الأخطاء:

- ❌ استخدام `alert()` بدلاً من notifications
- ❌ لا يوجد معالجة شاملة للأخطاء

---

## 🔄 آلية العمل الحالية بالتفصيل

### 1. **جلب أرصدة الإجازات**

#### الخطوات:

1. **جلب البيانات الأساسية**:

```javascript
// السطر 189-208
axios
  .get("get-emp-names") // جلب أسماء الموظفين
  .then((response) => {
    setEmpNames(response.data);
    setTstypes(response.data);
  });

axios
  .get("get-tasks-types-re") // جلب أنواع الإجازات
  .then((response) => {
    setTasksTypes(response.data);
  });
```

2. **جلب أرصدة الإجازات**:

```javascript
// السطر 212
axios.get("get-rest-tasks/" + currentYear).then((response) => {
  // معالجة البيانات
  // بناء الجدول
});
```

3. **معالجة البيانات**:

```javascript
// السطر 219-264
// بناء JSON يدوياً (يحتاج تحسين)
var vacData = "[";
emp.map((user) => {
  // بناء البيانات لكل موظف
  vacData += "{...},";
});
var json = JSON.parse(vacData.substring(0, vacData.length - 1) + "]");
setData(json);
```

---

### 2. **إضافة رصيد إجازة**

#### الخطوات:

1. **فتح النموذج**:

```javascript
// السطر 1331
onClick={() => setIsModalVisible(true)}
```

2. **ملء البيانات**:

- المستخدم يملأ النموذج العلوي (الإعدادات الأساسية)
- يمكنه إضافة موظفين يدوياً أو استخدام "جلب الموظفين"

3. **إرسال البيانات**:

```javascript
// السطر 465-494
const onFinish = () => {
  const formData = form.getFieldsValue();
  const globalData = gform.getFieldsValue();

  // دمج البيانات
  formData.tasks = formData.tasks.map((task) => ({
    ...task,
    period_type: task.period_type || globalData.period_type || "monthly",
  }));

  // إرسال
  axios.post("add-balance-tasks", formData);
};
```

---

### 3. **التقرير السنوي**

#### جلب البيانات:

```javascript
// السطر 331
axios.get("get-annualy-tasks-report/2/" + currentYear).then((response) => {
  setAnnTasks(response.data);
  setPAnnTasks(response.data);
});
```

#### البيانات المستلمة:

```javascript
{
  name: "اسم الموظف",
  job: "الوظيفة",
  category: "الإدارة",
  prev: 0,        // مرحل من العام الماضي (بالدقائق)
  curr: 0,        // رصيد العام الحالي (بالدقائق)
  trans: 0,       // رصيد محول (بالدقائق)
  m1: 0,          // استهلاك يناير (بالدقائق)
  m2: 0,          // استهلاك فبراير
  // ... حتى m12
}
```

#### الحسابات:

```javascript
// السطر 1772-1781
var op = Math.round(item.prev) + Math.round(item.curr) + Math.round(item.trans);
// op = الرصيد الافتتاحي

// السطر 1815-1820
var min = item["m" + (ind + 1)] / 60; // تحويل من دقائق إلى ساعات
totalg += min; // إجمالي الممنوح
```

---

## 🎯 التطوير المطلوب - ترحيل الإجازات

### 1. **الواجهة (Frontend)**

#### إضافة زر جديد:

```javascript
<Button
  onClick={handleOpenTransferModal}
  type="primary"
  icon={<SwapOutlined />}
  style={{ marginLeft: "5px" }}
>
  ترحيل الإجازات إلى السنة الجديدة
</Button>
```

#### Modal جديد للترحيل:

```javascript
<Modal
  title="ترحيل الإجازات إلى السنة الجديدة"
  visible={isTransferModalVisible}
  onOk={handleTransferLeaves}
  onCancel={() => setIsTransferModalVisible(false)}
>
  <Form form={transferForm}>
    <Form.Item
      name="from_year"
      label="السنة المصدر"
      rules={[{ required: true }]}
    >
      <DatePicker picker="year" />
    </Form.Item>

    <Form.Item name="to_year" label="السنة الهدف" rules={[{ required: true }]}>
      <DatePicker picker="year" />
    </Form.Item>

    <Form.Item name="vacation_types" label="أنواع الإجازات">
      <Select mode="multiple" placeholder="اختر أنواع الإجازات">
        {tasksTypes.map((type) => (
          <Option key={type.value} value={type.value}>
            {type.label}
          </Option>
        ))}
      </Select>
    </Form.Item>

    <Form.Item name="users" label="الموظفين">
      <Select mode="multiple" placeholder="اختر الموظفين (اتركه فارغاً للكل)">
        {empNames.map((emp) => (
          <Option key={emp.value} value={emp.value}>
            {emp.label}
          </Option>
        ))}
      </Select>
    </Form.Item>
  </Form>
</Modal>
```

#### دالة الترحيل:

```javascript
const handleTransferLeaves = async () => {
  try {
    const values = await transferForm.validateFields();
    setTransferring(true);

    const response = await axios.post("transfer-leaves-to-new-year", {
      from_year: values.from_year.format("YYYY"),
      to_year: values.to_year.format("YYYY"),
      vacation_types: values.vacation_types || null, // null = كل الأنواع
      users: values.users || null, // null = كل الموظفين
    });

    notification.success({
      message: "نجح الترحيل",
      description: `تم ترحيل ${response.data.transferred_count} سجل بنجاح`,
    });

    setIsTransferModalVisible(false);
    setUpdate(update + 1); // تحديث البيانات
  } catch (error) {
    notification.error({
      message: "فشل الترحيل",
      description: error.response?.data?.message || "حدث خطأ أثناء الترحيل",
    });
  } finally {
    setTransferring(false);
  }
};
```

---

### 2. **الخلفية (Backend)**

#### API Endpoint:

```php
// في routes/api.php
Route::post('/transfer-leaves-to-new-year', 'VacationAPIController@transferLeavesToNewYear');
```

#### Controller Method:

```php
// في VacationAPIController.php
public function transferLeavesToNewYear(Request $request) {
    try {
        $fromYear = $request->input('from_year');
        $toYear = $request->input('to_year');
        $vacationTypeIds = $request->input('vacation_types', null);
        $userIds = $request->input('users', null);

        // التحقق من البيانات
        if (!$fromYear || !$toYear) {
            return response()->json([
                'success' => false,
                'message' => 'يجب تحديد السنة المصدر والهدف'
            ], 400);
        }

        if ($fromYear >= $toYear) {
            return response()->json([
                'success' => false,
                'message' => 'السنة الهدف يجب أن تكون أكبر من السنة المصدر'
            ], 400);
        }

        // جلب الموظفين
        $usersQuery = User::where('status', 16);
        if ($userIds && count($userIds) > 0) {
            $usersQuery->whereIn('user_id', $userIds);
        }
        $users = $usersQuery->get();

        $transferredCount = 0;
        $errors = [];

        DB::beginTransaction();

        try {
            foreach ($users as $user) {
                // جلب الرصيد المتبقي من السنة السابقة
                $remainingBalances = $this->getRemainingBalanceForYear(
                    $user->user_id,
                    $fromYear,
                    $vacationTypeIds
                );

                foreach ($remainingBalances as $balance) {
                    // التحقق من وجود رصيد متبقي
                    if ($balance->remaining_minutes <= 0) {
                        continue;
                    }

                    // إنشاء سجل ترحيل في vacations_users
                    VacationUser::create([
                        'user_id' => $user->user_id,
                        'vacationstype_id' => $balance->vacationstype_id,
                        'amount' => $balance->remaining_minutes / 60, // تحويل من دقائق إلى ساعات
                        'type' => 34, // نوع: مرحل من العام الماضي (يجب التحقق من القيمة الصحيحة)
                        'period_type' => 'yearly',
                        'note' => "مرحل من سنة {$fromYear} إلى سنة {$toYear}",
                        'created_at' => now(),
                        'updated_at' => now(),
                    ]);

                    // تسجيل العملية في leave_transfers
                    LeaveTransfer::create([
                        'user_id' => $user->user_id,
                        'vacationstype_id' => $balance->vacationstype_id,
                        'amount' => $balance->remaining_minutes,
                        'from_year' => $fromYear,
                        'to_year' => $toYear,
                        'transfer_date' => now(),
                        'created_by' => auth()->id(),
                        'notes' => "ترحيل تلقائي من سنة {$fromYear}",
                    ]);

                    $transferredCount++;
                }
            }

            DB::commit();

            return response()->json([
                'success' => true,
                'message' => 'تم الترحيل بنجاح',
                'transferred_count' => $transferredCount
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            throw $e;
        }

    } catch (\Exception $e) {
        return response()->json([
            'success' => false,
            'message' => 'حدث خطأ أثناء الترحيل: ' . $e->getMessage()
        ], 500);
    }
}

private function getRemainingBalanceForYear($userId, $year, $vacationTypeIds = null) {
    // حساب الفترة السنوية (من 23 ديسمبر للسنة السابقة إلى 22 ديسمبر للسنة الحالية)
    $yearStart = ($year - 1) . "-12-23";
    $yearEnd = $year . "-12-22";

    // جلب الرصيد الممنوح
    $grantedQuery = DB::table('vacations_users')
        ->select('vacationstype_id', DB::raw('SUM(amount) * 60 as granted_minutes'))
        ->where('user_id', $userId)
        ->whereBetween('created_at', [$yearStart, $yearEnd])
        ->groupBy('vacationstype_id');

    if ($vacationTypeIds && count($vacationTypeIds) > 0) {
        $grantedQuery->whereIn('vacationstype_id', $vacationTypeIds);
    }

    $granted = $grantedQuery->get();

    // جلب الرصيد المستهلك
    $consumedQuery = DB::table('attendancelogs')
        ->select('type as vacationstype_id', DB::raw('SUM(TIME_TO_SEC(TIMEDIFF(leave_time, attendance_time))) as consumed_seconds'))
        ->where('user_id', $userId)
        ->where('type', '>', 0)
        ->whereBetween('date', [$yearStart, $yearEnd])
        ->groupBy('type');

    if ($vacationTypeIds && count($vacationTypeIds) > 0) {
        $consumedQuery->whereIn('type', $vacationTypeIds);
    }

    $consumed = $consumedQuery->get();

    // حساب الرصيد المتبقي
    $remaining = [];
    foreach ($granted as $grant) {
        $consumedItem = $consumed->firstWhere('vacationstype_id', $grant->vacationstype_id);
        $consumedMinutes = $consumedItem ? ($consumedItem->consumed_seconds / 60) : 0;
        $remainingMinutes = ($grant->granted_minutes) - $consumedMinutes;

        if ($remainingMinutes > 0) {
            $remaining[] = (object)[
                'vacationstype_id' => $grant->vacationstype_id,
                'remaining_minutes' => $remainingMinutes
            ];
        }
    }

    return $remaining;
}
```

---

### 3. **إنشاء جدول leave_transfers**

#### Migration:

```php
// في database/migrations
Schema::create('leave_transfers', function (Blueprint $table) {
    $table->id();
    $table->unsignedBigInteger('user_id');
    $table->unsignedBigInteger('vacationstype_id');
    $table->integer('amount'); // بالدقائق
    $table->year('from_year');
    $table->year('to_year');
    $table->dateTime('transfer_date');
    $table->unsignedBigInteger('created_by')->nullable();
    $table->text('notes')->nullable();
    $table->timestamps();

    $table->foreign('user_id')->references('user_id')->on('users');
    $table->foreign('vacationstype_id')->references('id')->on('vacationstypes');
    $table->foreign('created_by')->references('user_id')->on('users');

    $table->index(['user_id', 'from_year', 'to_year']);
});
```

#### Model:

```php
// في app/Models/LeaveTransfer.php
class LeaveTransfer extends Model
{
    protected $table = 'leave_transfers';

    protected $fillable = [
        'user_id',
        'vacationstype_id',
        'amount',
        'from_year',
        'to_year',
        'transfer_date',
        'created_by',
        'notes',
    ];

    public function user()
    {
        return $this->belongsTo(User::class, 'user_id');
    }

    public function vacationType()
    {
        return $this->belongsTo(VacationType::class, 'vacationstype_id');
    }
}
```

---

## 📋 ملخص التطويرات المطلوبة

### أولوية عالية 🔴

1. ✅ **نظام ترحيل الإجازات** - الميزة الأساسية المطلوبة
2. ✅ **تحسين بناء JSON** - إصلاح مشكلة في الكود الحالي
3. ✅ **إضافة زر طباعة التقرير السنوي** - تفعيل ميزة موجودة

### أولوية متوسطة 🟡

4. ✅ **تحسين معالجة الأخطاء** - استبدال alert بـ notifications
5. ✅ **إضافة تحقق من البيانات** - قبل الإرسال
6. ✅ **فلترة متقدمة** - حسب نوع الإجازة

### أولوية منخفضة 🟢

7. ✅ **تقارير إحصائية** - إحصائيات الترحيلات
8. ✅ **إشعارات** - عند اكتمال الترحيل
9. ✅ **سجل الترحيلات** - عرض تاريخ الترحيلات

---

## 🎓 الخلاصة

### ما هو موجود ✅

- نظام عرض أرصدة الإجازات
- إضافة رصيد إجازة
- خصم رصيد إجازة
- تقرير سنوي (لكن غير مستخدم)

### ما هو غير موجود ❌

- **نظام ترحيل الإجازات** (المطلوب الأساسي)
- تحسينات على الكود الحالي
- معالجة أخطاء محسنة

### الخطوات التالية 🚀

1. إنشاء جدول `leave_transfers`
2. إنشاء API endpoint للترحيل
3. إضافة واجهة المستخدم للترحيل
4. اختبار النظام
5. تحسينات إضافية

---

**تم إعداد هذا التقرير بواسطة**: AI Assistant  
**التاريخ**: 2024
