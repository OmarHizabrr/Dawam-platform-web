# 📊 خطوات قاعدة البيانات - نظام ترحيل الإجازات

## 🎯 نظرة عامة

هذا الملف يوضح جميع الخطوات المطلوبة في قاعدة البيانات لتنفيذ نظام ترحيل الإجازات.

---

## ✅ الخطوة 1: إنشاء جدول `leave_transfers` (جديد)

### الوصف:

جدول لتسجيل جميع عمليات ترحيل الإجازات من سنة إلى أخرى.

### Migration File:

**اسم الملف**: `YYYY_MM_DD_HHMMSS_create_leave_transfers_table.php`

**المسار**: `database/migrations/`

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateLeaveTransfersTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('leave_transfers', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('user_id');
            $table->unsignedBigInteger('vacationstype_id');
            $table->integer('amount'); // بالدقائق
            $table->year('from_year');
            $table->year('to_year');
            $table->dateTime('transfer_date');
            $table->unsignedBigInteger('created_by')->nullable();
            $table->text('notes')->nullable();
            $table->timestamps();

            // Foreign Keys
            $table->foreign('user_id')
                  ->references('user_id')
                  ->on('users')
                  ->onDelete('cascade');

            $table->foreign('vacationstype_id')
                  ->references('id')
                  ->on('vacationstypes')
                  ->onDelete('cascade');

            $table->foreign('created_by')
                  ->references('user_id')
                  ->on('users')
                  ->onDelete('set null');

            // Indexes
            $table->index(['user_id', 'from_year', 'to_year']);
            $table->index('vacationstype_id');
            $table->index('transfer_date');
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('leave_transfers');
    }
}
```

### SQL مباشر (إذا كنت لا تستخدم Migrations):

```sql
CREATE TABLE `leave_transfers` (
  `id` bigint(20) UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` bigint(20) UNSIGNED NOT NULL,
  `vacationstype_id` bigint(20) UNSIGNED NOT NULL,
  `amount` int(11) NOT NULL COMMENT 'بالدقائق',
  `from_year` year(4) NOT NULL,
  `to_year` year(4) NOT NULL,
  `transfer_date` datetime NOT NULL,
  `created_by` bigint(20) UNSIGNED DEFAULT NULL,
  `notes` text COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `leave_transfers_user_id_index` (`user_id`),
  KEY `leave_transfers_vacationstype_id_index` (`vacationstype_id`),
  KEY `leave_transfers_transfer_date_index` (`transfer_date`),
  KEY `leave_transfers_user_id_from_year_to_year_index` (`user_id`,`from_year`,`to_year`),
  CONSTRAINT `leave_transfers_user_id_foreign` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE,
  CONSTRAINT `leave_transfers_vacationstype_id_foreign` FOREIGN KEY (`vacationstype_id`) REFERENCES `vacationstypes` (`id`) ON DELETE CASCADE,
  CONSTRAINT `leave_transfers_created_by_foreign` FOREIGN KEY (`created_by`) REFERENCES `users` (`user_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## ✅ الخطوة 2: التحقق من جدول `vacations_users` وإضافة الحقول المطلوبة

### الوصف:

التحقق من وجود الحقول المطلوبة في جدول `vacations_users` وإضافتها إذا لم تكن موجودة.

### الحقول المطلوبة:

1. **`period_type`** (monthly/yearly) - نوع الفترة
2. **`transfer_from_year`** (اختياري) - السنة المصدر للترحيل
3. **`type`** - نوع الرصيد (يجب التحقق من القيمة المستخدمة للترحيل)

### Migration File:

**اسم الملف**: `YYYY_MM_DD_HHMMSS_add_period_type_to_vacations_users_table.php`

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class AddPeriodTypeToVacationsUsersTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::table('vacations_users', function (Blueprint $table) {
            // إضافة حقل period_type إذا لم يكن موجوداً
            if (!Schema::hasColumn('vacations_users', 'period_type')) {
                $table->enum('period_type', ['monthly', 'yearly'])
                      ->default('monthly')
                      ->after('amount')
                      ->comment('نوع الفترة: شهرية أو سنوية');
            }

            // إضافة حقل transfer_from_year إذا لم يكن موجوداً
            if (!Schema::hasColumn('vacations_users', 'transfer_from_year')) {
                $table->year('transfer_from_year')
                      ->nullable()
                      ->after('period_type')
                      ->comment('السنة المصدر في حالة الترحيل');
            }

            // إضافة فهرس لتحسين الأداء
            if (!Schema::hasIndex('vacations_users', 'vacations_users_period_type_index')) {
                $table->index('period_type');
            }
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::table('vacations_users', function (Blueprint $table) {
            if (Schema::hasColumn('vacations_users', 'period_type')) {
                $table->dropColumn('period_type');
            }
            if (Schema::hasColumn('vacations_users', 'transfer_from_year')) {
                $table->dropColumn('transfer_from_year');
            }
        });
    }
}
```

### SQL مباشر:

```sql
-- التحقق من وجود الحقول وإضافتها
ALTER TABLE `vacations_users`
ADD COLUMN IF NOT EXISTS `period_type` ENUM('monthly', 'yearly') DEFAULT 'monthly'
    COMMENT 'نوع الفترة: شهرية أو سنوية'
    AFTER `amount`;

ALTER TABLE `vacations_users`
ADD COLUMN IF NOT EXISTS `transfer_from_year` YEAR NULL
    COMMENT 'السنة المصدر في حالة الترحيل'
    AFTER `period_type`;

-- إضافة فهرس
CREATE INDEX IF NOT EXISTS `vacations_users_period_type_index`
    ON `vacations_users` (`period_type`);
```

### ملاحظة مهمة:

إذا كان جدول `vacations_users` لا يحتوي على حقل `type`، يجب إضافته أيضاً:

```sql
-- التحقق من وجود حقل type
ALTER TABLE `vacations_users`
ADD COLUMN IF NOT EXISTS `type` INT UNSIGNED NULL
    COMMENT 'نوع الرصيد (34 = مرحل من العام الماضي)'
    AFTER `vacationstype_id`;
```

---

## ✅ الخطوة 3: التحقق من هيكل الجداول الحالية

### جدول `vacations_users` (يجب أن يحتوي على):

```sql
-- عرض هيكل الجدول
DESCRIBE vacations_users;

-- أو
SHOW CREATE TABLE vacations_users;
```

**الحقول المتوقعة:**

- `id` - معرف السجل
- `user_id` - معرف الموظف
- `vacationstype_id` - معرف نوع الإجازة
- `amount` - المقدار (بالساعات عادة)
- `type` - نوع الرصيد (اختياري)
- `note` - ملاحظات (اختياري)
- `created_at` - تاريخ الإنشاء
- `updated_at` - تاريخ التحديث

### جدول `vacationstypes` (يجب أن يحتوي على):

```sql
DESCRIBE vacationstypes;
```

**الحقول المتوقعة:**

- `id` - معرف نوع الإجازة
- `name` - اسم نوع الإجازة
- `days` - عدد الأيام (اختياري)
- `att_report` - يظهر في التقرير (اختياري)

### جدول `users` (يجب أن يحتوي على):

```sql
DESCRIBE users;
```

**الحقول المتوقعة:**

- `user_id` - معرف الموظف
- `name` - اسم الموظف
- `status` - حالة الموظف (16 = نشط)
- `category_id` - معرف الإدارة

### جدول `attendancelogs` (يجب أن يحتوي على):

```sql
DESCRIBE attendancelogs;
```

**الحقول المتوقعة:**

- `id` - معرف السجل
- `user_id` - معرف الموظف
- `date` - التاريخ
- `attendance_time` - وقت الحضور
- `leave_time` - وقت الانصراف
- `type` - نوع الإجازة (0 = حضور عادي، >0 = نوع إجازة)

---

## ✅ الخطوة 4: إنشاء Model للجدول الجديد

### الملف: `app/Models/LeaveTransfer.php`

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class LeaveTransfer extends Model
{
    use HasFactory;

    protected $table = 'leave_transfers';

    protected $fillable = [
        'user_id',
        'vacationstype_id',
        'amount',
        'from_year',
        'to_year',
        'transfer_date',
        'created_by',
        'notes',
    ];

    protected $casts = [
        'transfer_date' => 'datetime',
        'from_year' => 'integer',
        'to_year' => 'integer',
        'amount' => 'integer',
    ];

    /**
     * العلاقة مع جدول الموظفين
     */
    public function user()
    {
        return $this->belongsTo(User::class, 'user_id', 'user_id');
    }

    /**
     * العلاقة مع جدول أنواع الإجازات
     */
    public function vacationType()
    {
        return $this->belongsTo(VacationType::class, 'vacationstype_id', 'id');
    }

    /**
     * العلاقة مع المستخدم الذي أنشأ الترحيل
     */
    public function creator()
    {
        return $this->belongsTo(User::class, 'created_by', 'user_id');
    }

    /**
     * Scope للحصول على الترحيلات حسب السنة
     */
    public function scopeForYear($query, $year)
    {
        return $query->where('to_year', $year);
    }

    /**
     * Scope للحصول على الترحيلات حسب الموظف
     */
    public function scopeForUser($query, $userId)
    {
        return $query->where('user_id', $userId);
    }
}
```

---

## ✅ الخطوة 5: التحقق من قيمة `type` للترحيل

### الوصف:

يجب التحقق من القيمة المستخدمة في حقل `type` في جدول `vacations_users` للإشارة إلى "مرحل من العام الماضي".

### الاستعلام:

```sql
-- عرض جميع القيم المختلفة في حقل type
SELECT DISTINCT type, COUNT(*) as count
FROM vacations_users
WHERE type IS NOT NULL
GROUP BY type;

-- أو التحقق من جدول types
SELECT * FROM types WHERE parent_id = 34;
```

### ملاحظة:

من الكود السابق، يبدو أن القيمة `34` تستخدم للإشارة إلى نوع الرصيد "مرحل من العام الماضي". يجب التحقق من ذلك في قاعدة البيانات.

---

## ✅ الخطوة 6: إنشاء Stored Procedure (اختياري)

### الوصف:

يمكن إنشاء Stored Procedure لحساب الرصيد المتبقي لتسهيل الاستعلامات.

```sql
DELIMITER //

CREATE PROCEDURE GetRemainingBalance(
    IN p_user_id INT,
    IN p_year INT,
    IN p_vacationstype_id INT
)
BEGIN
    DECLARE v_year_start DATE;
    DECLARE v_year_end DATE;

    -- حساب الفترة السنوية (من 23 ديسمبر للسنة السابقة إلى 22 ديسمبر للسنة الحالية)
    SET v_year_start = DATE(CONCAT(p_year - 1, '-12-23'));
    SET v_year_end = DATE(CONCAT(p_year, '-12-22'));

    -- حساب الرصيد المتبقي
    SELECT
        vu.vacationstype_id,
        SUM(vu.amount) * 60 as granted_minutes,
        COALESCE(SUM(TIME_TO_SEC(TIMEDIFF(al.leave_time, al.attendance_time))), 0) / 60 as consumed_minutes,
        (SUM(vu.amount) * 60) - (COALESCE(SUM(TIME_TO_SEC(TIMEDIFF(al.leave_time, al.attendance_time))), 0) / 60) as remaining_minutes
    FROM vacations_users vu
    LEFT JOIN attendancelogs al
        ON al.user_id = vu.user_id
        AND al.type = vu.vacationstype_id
        AND al.date BETWEEN v_year_start AND v_year_end
        AND al.type > 0
    WHERE vu.user_id = p_user_id
        AND vu.created_at BETWEEN v_year_start AND v_year_end
        AND (p_vacationstype_id IS NULL OR vu.vacationstype_id = p_vacationstype_id)
    GROUP BY vu.vacationstype_id
    HAVING remaining_minutes > 0;
END //

DELIMITER ;
```

---

## 📋 قائمة التحقق (Checklist)

### قبل البدء:

- [ ] عمل نسخة احتياطية من قاعدة البيانات
- [ ] التحقق من هيكل الجداول الحالية
- [ ] التحقق من وجود Foreign Keys في الجداول المرتبطة

### التنفيذ:

- [ ] إنشاء جدول `leave_transfers`
- [ ] إضافة حقل `period_type` إلى `vacations_users`
- [ ] إضافة حقل `transfer_from_year` إلى `vacations_users`
- [ ] التحقق من وجود حقل `type` في `vacations_users`
- [ ] إنشاء Model `LeaveTransfer`
- [ ] اختبار الاستعلامات الأساسية

### بعد التنفيذ:

- [ ] التحقق من عمل Foreign Keys
- [ ] التحقق من عمل Indexes
- [ ] اختبار إدراج سجل تجريبي
- [ ] اختبار الاستعلامات المعقدة

---

## 🔍 استعلامات التحقق

### 1. التحقق من إنشاء الجدول:

```sql
SHOW TABLES LIKE 'leave_transfers';
```

### 2. التحقق من هيكل الجدول:

```sql
DESCRIBE leave_transfers;
```

### 3. التحقق من Foreign Keys:

```sql
SELECT
    CONSTRAINT_NAME,
    TABLE_NAME,
    COLUMN_NAME,
    REFERENCED_TABLE_NAME,
    REFERENCED_COLUMN_NAME
FROM INFORMATION_SCHEMA.KEY_COLUMN_USAGE
WHERE TABLE_NAME = 'leave_transfers'
    AND TABLE_SCHEMA = DATABASE();
```

### 4. التحقق من Indexes:

```sql
SHOW INDEXES FROM leave_transfers;
```

### 5. التحقق من الحقول المضافة:

```sql
-- التحقق من حقل period_type
SHOW COLUMNS FROM vacations_users LIKE 'period_type';

-- التحقق من حقل transfer_from_year
SHOW COLUMNS FROM vacations_users LIKE 'transfer_from_year';
```

---

## ⚠️ ملاحظات مهمة

1. **النسخ الاحتياطي**:

   - دائماً قم بعمل نسخة احتياطية قبل أي تعديلات على قاعدة البيانات

2. **البيئة**:

   - اختبر على بيئة التطوير أولاً
   - ثم طبّق على بيئة الإنتاج

3. **الأداء**:

   - Indexes مهمة جداً لتحسين الأداء
   - خاصة على الحقول المستخدمة في JOIN و WHERE

4. **البيانات الموجودة**:
   - إذا كان جدول `vacations_users` يحتوي على بيانات موجودة، يجب تحديث حقل `period_type` للبيانات القديمة:
   ```sql
   UPDATE vacations_users
   SET period_type = 'monthly'
   WHERE period_type IS NULL;
   ```

---

## 🚀 الخطوات التالية

بعد إكمال خطوات قاعدة البيانات:

1. ✅ إنشاء API Endpoint في Backend
2. ✅ إنشاء واجهة المستخدم في Frontend
3. ✅ اختبار النظام بالكامل

---

**تاريخ الإعداد**: 2024  
**الإصدار**: 1.0
